/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Uncomment for static export (no server needed for demo)
  // output: 'export',
  // distDir: 'dist',
}
module.exports = nextConfig
